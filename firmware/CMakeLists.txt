# ESP-IDF Project CMakeLists.txt
cmake_minimum_required(VERSION 3.16)

# Set the target chip
set(IDF_TARGET "esp32s3")

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(manipulator_firmware)
